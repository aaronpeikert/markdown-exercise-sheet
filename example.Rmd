---
title: "Example"
author: "Aaron Peikert"
date: "2/12/2019"
output:
  html_document: default
  pdf_document:
    template: standard.tex
    extra_dependencies: #may clash with template, if you specify here something that is allready in template it fails
      lmodern: null
params:
  solution: true
  code: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = params$code)
```

# SEM

1. Should you run trust that the default SEM-Options in `MPLUS` or `lavaan` are what I have in Mind?

```{asis, echo = params$solution}
Answer: NO!
```

# Cars

Fit a regession on mtcars.

```{r, include=params$code}
fit <- lm(mpg ~ hp + wt, data = mtcars)
summary(fit)
```

